[tool.poetry]
name = "book-catalog"
version = "0.1.0"
package-mode = false

[tool.poetry.dependencies]
python = "3.13.13"
sqlalchemy = "^2.0.49"
fastapi = "^0.136.0"
cryptography = "^46.0.7"
werkzeug = "^3.1.8"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
