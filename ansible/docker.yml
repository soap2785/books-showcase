- name: Prepare docker
  hosts: all
  tasks:
    - name: Install Docker and Compose from Debian repos
      ansible.builtin.apt:
        name:
          - docker.io
          - docker-compose-v2
          - curl
        state: present
        update_cache: yes
        lock_timeout: 600
